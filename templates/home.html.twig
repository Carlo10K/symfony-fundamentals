{% extends "base.html.twig" %}

{% block body %}
{# dump(search) #}
    <h1>Welcome, pagina home</h1>
    <div>
        {% for comment in comments %}
            <p>{{ comment.body }}</p>
        {% endfor %}
    </div>
{% endblock %}